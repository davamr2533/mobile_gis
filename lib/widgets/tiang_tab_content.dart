import 'package:flutter/material.dart';

class TiangTabContent extends StatelessWidget {
  const TiangTabContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Data Tiang akan tampil di sini"),
    );
  }
}